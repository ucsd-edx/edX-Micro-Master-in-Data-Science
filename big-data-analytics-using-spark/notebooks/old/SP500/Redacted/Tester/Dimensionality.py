import pickle

from Tester import *
    
def exercise_1(pickleFile, func_student):
    checkExerciseFromPickle(pickleFile, func_student,TestNumber,'ex_1', multiInputs=True)
    
def exercise_2(pickleFile, func_student):
    checkExerciseFromPickle(pickleFile, func_student,TestNumber,'ex_2', multiInputs=True)
    
def exercise_3(pickleFile, func_student):
    checkExerciseFromPickle(pickleFile, func_student,TestNumber,'ex_3', multiInputs=True)